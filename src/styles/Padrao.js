import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    ex: {
      paddingHorizontal: 15,
      marginVertical: 5,
      borderRadius: 10,
      borderColor: '#222',
      fontSize: 24,
      fontWeight: 'bold'
    }
  });